# Minimum version required for the CMake build system
cmake_minimum_required(VERSION 3.5)

# Include ESP-IDF specific build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define the project
project(M5Stack_Bluetooth_Spam)
